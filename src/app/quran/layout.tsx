import Layout from 'modules/quran/templates/main.template';
import { withMetadata } from 'packages/utils/metadata';

export const metadata = withMetadata({
  title: 'Nun - Quran',
  description: 'Quran terjemah',
});

export default Layout;
